# ===================================
#  List Column Definitions
# ===================================

columns:
    id:
        label: jumplink.slideshow::lang.columns.slideshow.id
        searchable: true
    name:
        label: jumplink.slideshow::lang.columns.slideshow.name
        searchable: true
    slides_count:
        label: jumplink.slideshow::lang.columns.slideshow.slides_count
        relation: slides_count
        valueFrom: count
        default: 0
